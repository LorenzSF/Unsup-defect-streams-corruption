# Job C — Real-IAD on wICE/HPC. Inherits the Colab Job C config and only
# overrides the output dir (to /scratch) and bumps num_workers (the A100 node
# has more cores than Colab; 4 is safe with 1 GPU).

_extends: ./colab_jobC_realiad.yaml

run:
  run_name: "jobC_realiad_wice"
  output_dir: "/scratch/leuven/381/vsc38124/runs"

runtime:
  num_workers: 4
